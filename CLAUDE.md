# CLAUDE.md — Claude Code notes

Start with **[`AGENTS.md`](AGENTS.md)**; everything there applies. Claude-specific:

@./AGENTS.md

- **Repo memory dir:**
  `~/.claude/projects/-mnt-c-Users-sdl2-Projects-sdl2-gibbie-server/memory/`.
- From WSL there is no `uv` on PATH; use `/mnt/c/SDL_Tools/uv.exe`.
- The Gibbie PC is reached with `ssh sdl2@100.64.254.17` (robot key,
  elevated account). Service control there needs an elevated shell the human
  runs; stage scripts and hand over one-liners.
